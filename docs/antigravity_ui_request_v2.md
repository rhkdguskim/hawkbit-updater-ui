# hawkBit 운영 대시보드 UI 개선 요청서
## Antigravity Development Request

**문서 버전:** v1.0  
**작성일:** 2026-01-01  
**요청자:** 운영팀  

---

## 1. 개요

### 1.1 목적
기존 hawkBit REST API를 활용하여 운영 대시보드 UI를 기획 의도에 맞게 개선합니다.

### 1.2 제약 사항
- ⚠️ **Backend API 변경 불가** - 기존 hawkBit REST API만 사용
- Frontend에서 데이터 가공/집계 처리
- 기존 API 조합으로 필요한 정보 도출

### 1.3 기획 핵심 원칙 (재확인)
```
✓ 30초 내 시스템 상태 파악 가능
✓ 롤아웃 중심 뷰 (Rollout-centric view)
✓ 즉각적인 롤아웃 제어 가능
✓ 의사결정 & 제어 > 분석
✓ 값이 0이거나 비어있는 위젯 표시 금지
```

---

## 2. 사용 가능한 API 목록

### 2.1 Rollout 관련 API

| API | Method | 용도 |
|-----|--------|------|
| `/rest/v1/rollouts` | GET | 롤아웃 목록 조회 (q 파라미터로 필터링 가능) |
| `/rest/v1/rollouts/{rolloutId}` | GET | 롤아웃 상세 조회 |
| `/rest/v1/rollouts/{rolloutId}/deploygroups` | GET | 롤아웃 배포 그룹 목록 |
| `/rest/v1/rollouts/{rolloutId}/deploygroups/{groupId}` | GET | 배포 그룹 상세 |
| `/rest/v1/rollouts/{rolloutId}/deploygroups/{groupId}/targets` | GET | 그룹 내 타겟 목록 |
| `/rest/v1/rollouts/{rolloutId}/pause` | POST | 롤아웃 일시정지 |
| `/rest/v1/rollouts/{rolloutId}/resume` | POST | 롤아웃 재개 |
| `/rest/v1/rollouts/{rolloutId}/start` | POST | 롤아웃 시작 |
| `/rest/v1/rollouts/{rolloutId}/approve` | POST | 롤아웃 승인 |
| `/rest/v1/rollouts/{rolloutId}/deny` | POST | 롤아웃 거부 |
| `/rest/v1/rollouts/{rolloutId}/triggerNextGroup` | POST | 다음 그룹 트리거 |
| `/rest/v1/rollouts/{rolloutId}/retry` | POST | 롤아웃 재시도 |

### 2.2 Target 관련 API

| API | Method | 용도 |
|-----|--------|------|
| `/rest/v1/targets` | GET | 타겟 목록 조회 (q 파라미터로 필터링) |
| `/rest/v1/targets/{targetId}` | GET | 타겟 상세 조회 |
| `/rest/v1/targets/{targetId}/actions` | GET | 타겟의 액션 목록 |
| `/rest/v1/targets/{targetId}/actions/{actionId}` | GET | 액션 상세 |
| `/rest/v1/targets/{targetId}/actions/{actionId}/status` | GET | 액션 상태 이력 |

### 2.3 Action 관련 API

| API | Method | 용도 |
|-----|--------|------|
| `/rest/v1/actions` | GET | 액션 목록 조회 (q 파라미터로 필터링) |
| `/rest/v1/actions/{actionId}` | GET | 액션 상세 조회 |

### 2.4 주요 응답 필드 (활용 가능)

**Rollout (MgmtRolloutResponseBody):**
```
- id, name, description
- status: creating, ready, paused, running, finished, error 등
- totalTargets: 전체 타겟 수
- totalTargetsPerStatus: {running, notstarted, scheduled, cancelled, finished, error}
- totalGroups: 전체 그룹 수
- distributionSetId
- targetFilterQuery
- startAt, forcetime
- deleted, dynamic
```

**RolloutGroup (MgmtRolloutGroupResponseBody):**
```
- id, name, description
- status: ready, running, finished, error 등
- totalTargets
- totalTargetsPerStatus: {running, notstarted, scheduled, cancelled, finished, error}
- targetPercentage
- successCondition, errorCondition
- successAction, errorAction
```

**Target (MgmtTarget):**
```
- controllerId, name
- updateStatus: in_sync, pending, error, registered 등
- lastControllerRequestAt
- pollStatus: {lastRequestAt, nextExpectedRequestAt, overdue}
- ipAddress, address
- targetType, targetTypeName
```

**Action (MgmtAction):**
```
- id, type (update, cancel)
- status: running, pending, finished, error, cancelled 등
- detailStatus
- forceType: soft, forced, timeforced, downloadonly
- weight
- rollout, rolloutName
- lastStatusCode
- createdAt, lastModifiedAt
```

---

## 3. UI 개선 요청 상세

### 3.1 TOP SECTION: Decision Layer

---

#### [UI-01] Health Summary Widget (신규)

**목적:** "지금 시스템이 안전한가?" 즉시 답변

**데이터 조합:**
```javascript
// 1. 롤아웃 상태 조회
GET /rest/v1/rollouts?q=status==paused,status==running&limit=100

// 2. 타겟 전체 수
GET /rest/v1/targets?limit=1  // total 필드로 전체 수 확인

// 3. 에러 상태 액션 (최근 1시간)
GET /rest/v1/actions?q=status==error&limit=100
// createdAt 필드로 1시간 이내 필터링 (Frontend)
```

**Frontend 계산 로직:**
```javascript
function calculateHealthStatus(rollouts, actions, targets) {
  const pausedRollouts = rollouts.filter(r => r.status === 'paused').length;
  const recentErrorActions = actions.filter(a => 
    a.status === 'error' && 
    (Date.now() - a.createdAt) < 3600000 // 1시간
  );
  const errorRate = recentErrorActions.length / targets.total;
  
  // 상태 판정
  if (pausedRollouts > 0 || errorRate > 0.05) return 'CRITICAL';
  if (errorRate > 0.02) return 'WARNING';
  return 'SAFE';
}
```

**UI 표시:**
```
┌─────────────────────────────────────────────────┐
│  🔴 CRITICAL                                    │
│  ─────────────────────────────────────────────  │
│  총 타겟: 100        업데이트 중: 49            │
│  에러율 (1h): 2.1%   일시정지 롤아웃: 3         │
│  ─────────────────────────────────────────────  │
│  ⚠️ 3개 롤아웃 일시정지됨                       │
└─────────────────────────────────────────────────┘
```

**상태별 스타일:**
- SAFE: 녹색 배지, 녹색 테두리
- WARNING: 주황색 배지, 주황색 테두리  
- CRITICAL: 빨간색 배지, 빨간색 테두리, 펄스 애니메이션

---

#### [UI-02] Action Required Widget (신규)

**목적:** 즉시 조치 필요 항목 집중 표시

**데이터 조합:**
```javascript
// 1. 일시정지된 롤아웃
GET /rest/v1/rollouts?q=status==paused&limit=10

// 2. 에러 상태 롤아웃
GET /rest/v1/rollouts?q=status==error&limit=10

// 3. 오래된 running 액션 (지연 감지)
GET /rest/v1/actions?q=status==running&sort=createdAt:ASC&limit=20
// createdAt이 24시간 이상 된 항목 필터링 (Frontend)

// 4. 오프라인 타겟
GET /rest/v1/targets?limit=50
// pollStatus.overdue == true 필터링 (Frontend)
```

**Frontend 로직:**
```javascript
function generateActionItems(pausedRollouts, errorRollouts, staleActions, offlineTargets) {
  const items = [];
  
  // HIGH 우선순위
  pausedRollouts.forEach(r => items.push({
    type: 'ROLLOUT',
    severity: 'HIGH',
    title: `롤아웃 "${r.name}" 일시정지됨`,
    entityId: r.id,
    action: 'resume'
  }));
  
  errorRollouts.forEach(r => items.push({
    type: 'ROLLOUT', 
    severity: 'HIGH',
    title: `롤아웃 "${r.name}" 에러 발생`,
    entityId: r.id,
    action: 'view'
  }));
  
  // MEDIUM 우선순위
  staleActions.forEach(a => items.push({
    type: 'ACTION',
    severity: 'MEDIUM',
    title: `액션 #${a.id} ${getDaysStale(a.createdAt)}일간 지연`,
    entityId: a.id
  }));
  
  // 심각도 순 정렬 후 상위 5개
  return items
    .sort((a, b) => severityOrder[a.severity] - severityOrder[b.severity])
    .slice(0, 5);
}
```

**UI 표시:**
```
┌─────────────────────────────────────────────────┐
│  ⚠️ 즉시 조치 필요 (3)                          │
│  ─────────────────────────────────────────────  │
│  🔴 롤아웃 "Firmware-v2.3" 일시정지   [재개 ▶]  │
│  🔴 롤아웃 "Security-Patch" 에러      [보기]    │
│  🟡 액션 #1234 - 9일간 지연           [보기]    │
│  ─────────────────────────────────────────────  │
│  조치 필요 없음 시 이 위젯 숨김                 │
└─────────────────────────────────────────────────┘
```

**클릭 동작:**
- 롤아웃 항목: `/rollouts/{id}` 페이지로 이동
- 액션 항목: `/targets/{targetId}/actions/{actionId}` 또는 액션 상세 모달
- [재개] 버튼: `POST /rollouts/{id}/resume` 호출 후 새로고침

---

### 3.2 MIDDLE SECTION: Control Layer

---

#### [UI-03] Active Rollouts Widget (개선)

**현재 문제:**
- ❌ 롤아웃 이름 잘림
- ❌ 제어 버튼 없음
- ❌ 에러율/그룹 진행 상황 없음

**데이터 조회:**
```javascript
// 활성 롤아웃 (RUNNING 또는 PAUSED)
GET /rest/v1/rollouts?q=status==running,status==paused&limit=3&sort=lastModifiedAt:DESC

// 각 롤아웃의 그룹 정보
GET /rest/v1/rollouts/{rolloutId}/deploygroups
```

**UI 개선안:**
```
┌─────────────────────────────────────────────────────────────┐
│  🟡 활성 롤아웃 (3)                              전체 보기 ▶ │
├─────────────────────────────────────────────────────────────┤
│  ⏸️ Production-Firmware-v2.3.1                    [PAUSED]  │
│  ───────────────────────────────────────────────────────── │
│  진행률: ████████░░░░░░░░░░░░░░░░░░░░░░  0% (0/49)         │
│  그룹:   ■□□□□ (1/5)                                       │
│  상태:   ✓ 0  ⏳ 49  ✕ 0                                   │
│  ───────────────────────────────────────────────────────── │
│  [▶ 재개]  [⏹ 중지]  [📋 상세]  [▼ 그룹 보기]              │
├─────────────────────────────────────────────────────────────┤
│  ▶️ IoT-Sensor-Security-Patch                    [RUNNING] │
│  ───────────────────────────────────────────────────────── │
│  진행률: ██████████████░░░░░░░░░░░░░░░░  35% (35/99)       │
│  그룹:   ■■■□□ (3/5)                                       │
│  상태:   ✓ 35  ⏳ 64  ✕ 0                                  │
│  ───────────────────────────────────────────────────────── │
│  [⏸ 일시정지]  [📋 상세]  [▼ 그룹 보기]                    │
└─────────────────────────────────────────────────────────────┘
```

**버튼 액션:**

| 버튼 | API 호출 | 조건 |
|------|----------|------|
| ▶ 재개 | `POST /rollouts/{id}/resume` | status == paused |
| ⏸ 일시정지 | `POST /rollouts/{id}/pause` | status == running |
| ▶ 시작 | `POST /rollouts/{id}/start` | status == ready |
| 📋 상세 | 페이지 이동 `/rollouts/{id}` | 항상 |
| ▼ 그룹 보기 | 확장/축소 (Drilldown) | 항상 |

**확인 다이얼로그:**
```
┌─────────────────────────────────────────┐
│  롤아웃 재개 확인                        │
│  ─────────────────────────────────────  │
│  "Production-Firmware-v2.3.1"           │
│  롤아웃을 재개하시겠습니까?              │
│                                         │
│  49개 타겟에 대한 배포가 계속됩니다.     │
│                                         │
│            [취소]  [재개 ▶]              │
└─────────────────────────────────────────┘
```

---

#### [UI-04] Rollout Drilldown View (신규)

**목적:** 롤아웃 카드 확장 시 그룹별 상세 표시

**데이터 조회:**
```javascript
GET /rest/v1/rollouts/{rolloutId}/deploygroups?limit=20
```

**UI 표시 (확장 시):**
```
┌─────────────────────────────────────────────────────────────┐
│  ▼ Production-Firmware-v2.3.1                               │
│  ═══════════════════════════════════════════════════════════│
│  │ Group 1: Canary        ✅ 완료    100% (10/10)   0 에러 │
│  │ Group 2: Early         ⏳ 진행중   60% (12/20)   1 에러 │
│  │ Group 3: Main          ⏸️ 대기     0%  (0/100)   - 에러 │
│  │ Group 4: Late          ○ 예정                           │
│  │ Group 5: Final         ○ 예정                           │
│  └──────────────────────────────────────────────────────────│
│  그룹 클릭 시 해당 그룹의 타겟 목록으로 이동                 │
└─────────────────────────────────────────────────────────────┘
```

**그룹 상태 표시:**
```javascript
function getGroupStatusIcon(group) {
  switch(group.status) {
    case 'finished': return '✅';
    case 'running': return '⏳';
    case 'paused': return '⏸️';
    case 'error': return '❌';
    default: return '○';
  }
}
```

**클릭 동작:**
- 그룹 행 클릭 → `GET /rollouts/{rolloutId}/deploygroups/{groupId}/targets`
- 또는 필터된 타겟 목록 페이지로 이동

---

#### [UI-05] In-Progress Updates Widget (개선)

**현재 문제:**
- ❌ 상태 메시지 잘림
- ❌ 진행 단계 불명확
- ❌ 9일 지연 경고 없음
- ❌ 연관 롤아웃 정보 없음

**데이터 조회:**
```javascript
// Running 상태 액션
GET /rest/v1/actions?q=status==running&limit=10&sort=createdAt:ASC

// 각 액션의 타겟 정보 (필요시)
// _links.target.href 에서 타겟 정보 조회 가능
```

**UI 개선안:**
```
┌─────────────────────────────────────────────────────────────┐
│  🔄 진행 중인 업데이트 (10)                      필터 ▼     │
│  ⚠️ 5개 항목 24시간 이상 지연                               │
├─────────────────────────────────────────────────────────────┤
│  🔴 sim-device-034                               9일 지연   │
│      롤아웃: Production-Firmware-v2.3.1                     │
│      상태: running (update)                                 │
│      시작: 2025-12-23 10:30                                │
│                                        [↻ 재시도] [✕ 취소] │
├─────────────────────────────────────────────────────────────┤
│  🟡 sim-device-045                               2일 지연   │
│      롤아웃: IoT-Sensor-Security-Patch                      │
│      상태: running (update)                                 │
│      시작: 2025-12-30 14:20                                │
│                                        [↻ 재시도] [✕ 취소] │
├─────────────────────────────────────────────────────────────┤
│  🟢 sim-device-023                               방금 전    │
│      롤아웃: Gateway-Update                                 │
│      상태: running (update)                                 │
│      시작: 2026-01-01 09:45                                │
│                                                  [✕ 취소] │
└─────────────────────────────────────────────────────────────┘
```

**지연 시간 계산:**
```javascript
function getDelayStatus(action) {
  const elapsed = Date.now() - action.createdAt;
  const days = Math.floor(elapsed / (24 * 60 * 60 * 1000));
  
  if (days >= 7) return { color: 'red', text: `${days}일 지연`, icon: '🔴' };
  if (days >= 1) return { color: 'orange', text: `${days}일 지연`, icon: '🟡' };
  if (elapsed > 3600000) return { color: 'orange', text: `${Math.floor(elapsed/3600000)}시간`, icon: '🟡' };
  return { color: 'green', text: '방금 전', icon: '🟢' };
}
```

**롤아웃 정보 표시:**
- `action.rolloutName` 필드 활용
- 없는 경우 `action._links.rollout.name` 활용

---

### 3.3 BOTTOM SECTION: Monitoring Layer

---

#### [UI-06] Status Trend Chart (개선)

**현재 문제:**
- 단순 속도만 표시
- 이벤트 마커 없음

**데이터 조회:**
```javascript
// 시간대별 집계는 Frontend에서 처리
// 최근 24시간 액션 조회
GET /rest/v1/actions?limit=500&sort=createdAt:DESC

// 롤아웃 이벤트 (시작/일시정지 등)
GET /rest/v1/rollouts?limit=50&sort=lastModifiedAt:DESC
```

**Frontend 데이터 가공:**
```javascript
function aggregateByHour(actions) {
  const hourlyData = {};
  
  actions.forEach(action => {
    const hour = new Date(action.createdAt).setMinutes(0, 0, 0);
    if (!hourlyData[hour]) {
      hourlyData[hour] = { success: 0, running: 0, error: 0 };
    }
    hourlyData[hour][action.status]++;
  });
  
  return Object.entries(hourlyData).map(([hour, counts]) => ({
    timestamp: parseInt(hour),
    ...counts
  }));
}
```

**UI 표시:**
```
┌─────────────────────────────────────────────────────────────┐
│  📊 상태 추이 (24시간)                                      │
│  ─────────────────────────────────────────────────────────  │
│  60│     ▶                    ⏸                            │
│    │    ╱╲    ████            │                             │
│  40│   ╱  ╲  ██████          ███                            │
│    │  ╱    ╲████████        █████                           │
│  20│ ╱      ████████████████████████                        │
│    │╱                                                       │
│   0└────────────────────────────────────────────────────    │
│     00:00  04:00  08:00  12:00  16:00  20:00  24:00         │
│  ─────────────────────────────────────────────────────────  │
│  ■ 성공  ■ 진행중  ■ 에러   ▶ 롤아웃 시작  ⏸ 일시정지      │
└─────────────────────────────────────────────────────────────┘
```

---

#### [UI-07] Action Activity Widget (개선)

**목적:** 시스템 부하 및 병목 감지

**데이터 조회:**
```javascript
// 전체 액션 통계
GET /rest/v1/actions?limit=1  // total 필드

// Running 액션 (대기 중)
GET /rest/v1/actions?q=status==running&limit=1

// 최근 완료된 액션 (완료 시간 계산용)
GET /rest/v1/actions?q=status==finished&sort=lastModifiedAt:DESC&limit=50
```

**Frontend 계산:**
```javascript
function calculateActivityMetrics(runningActions, finishedActions) {
  // 평균 완료 시간 계산
  const completionTimes = finishedActions.map(a => 
    a.lastModifiedAt - a.createdAt
  );
  const avgCompletionMs = completionTimes.reduce((a, b) => a + b, 0) / completionTimes.length;
  const avgCompletionMin = Math.round(avgCompletionMs / 60000);
  
  return {
    waitingActions: runningActions.total,
    avgCompletionTime: avgCompletionMin,
    isBottleneck: runningActions.total > 30 || avgCompletionMin > 15
  };
}
```

**UI 표시:**
```
┌─────────────────────────────────────────────────────────────┐
│  📈 액션 활동                                               │
│  ─────────────────────────────────────────────────────────  │
│  대기 중 액션:    49 ⚠️ (임계: 30)                          │
│  평균 완료 시간:  12분 (정상: < 15분)                       │
│  ─────────────────────────────────────────────────────────  │
│  ⚠️ 병목 현상 감지됨                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 제거/숨김 대상 위젯

기획서 원칙에 따라:

| 현재 위젯 | 조치 | 사유 |
|-----------|------|------|
| 취소된 액션 (24h) | **제거** | 의도적 행위, 저가치 |
| 신규 타겟 추이 | **제거** | Out of scope (자산관리) |
| 현재 속도 | **제거** | Action Activity로 통합 |
| 타겟 유형 분포 | **제거** | Out of scope (세분화 분석) |
| 완전성 | **제거** | 의미 모호 |
| 액션 처리량 추이 | **대체** | Status Trend로 통합 |

**조건부 숨김:**
```javascript
// 값이 0이거나 의미 없을 때 위젯 숨김
if (actionRequiredItems.length === 0) hide('action-required-widget');
if (activeRollouts.length === 0) hide('active-rollouts-widget');
if (runningActions.length === 0) hide('in-progress-widget');
```

---

## 5. 레이아웃 구조

### 5.1 목표 레이아웃

```
┌─────────────────────────────────────────────────────────────────────┐
│                        TOP: DECISION LAYER                          │
│ ┌───────────────┬─────────────────────┬───────────────────────────┐ │
│ │    HEALTH     │   ACTION REQUIRED   │      (여유 공간 또는      │ │
│ │    SUMMARY    │   (즉시 조치 필요)   │       추후 확장용)        │ │
│ │   🔴 CRITICAL │   ⚠️ 3 items        │                           │ │
│ └───────────────┴─────────────────────┴───────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│                       MIDDLE: CONTROL LAYER                         │
│ ┌─────────────────────────────────┬─────────────────────────────┐   │
│ │       ACTIVE ROLLOUTS           │    IN-PROGRESS UPDATES      │   │
│ │       (제어 가능)                │    (디바이스 상태)          │   │
│ │    [▶Resume] [⏸Pause]           │    [↻Retry] [✕Cancel]       │   │
│ │    + Drilldown 지원              │                             │   │
│ └─────────────────────────────────┴─────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────┤
│                      BOTTOM: MONITORING LAYER                       │
│ ┌─────────────────────────────────┬─────────────────────────────┐   │
│ │       STATUS TREND              │      ACTION ACTIVITY        │   │
│ │       (24시간 추이)              │      (시스템 부하)          │   │
│ └─────────────────────────────────┴─────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2 높이 비율

| Section | 비율 | 설명 |
|---------|------|------|
| TOP (Decision) | 20% | 최소 높이로 핵심 정보만 |
| MIDDLE (Control) | 50% | 메인 작업 영역 |
| BOTTOM (Monitoring) | 30% | 참고용 추이 |

---

## 6. API 호출 전략

### 6.1 초기 로딩

```javascript
async function loadDashboard() {
  const [
    rollouts,
    targets,
    actions,
    runningActions
  ] = await Promise.all([
    fetch('/rest/v1/rollouts?q=status==running,status==paused&limit=10'),
    fetch('/rest/v1/targets?limit=1'),  // total만 필요
    fetch('/rest/v1/actions?q=status==error&limit=100'),
    fetch('/rest/v1/actions?q=status==running&limit=50')
  ]);
  
  // 위젯별 데이터 분배 및 렌더링
}
```

### 6.2 자동 새로고침

```javascript
// 30초마다 자동 새로고침
setInterval(loadDashboard, 30000);

// 또는 액션 수행 후 즉시 새로고침
async function resumeRollout(rolloutId) {
  await fetch(`/rest/v1/rollouts/${rolloutId}/resume`, { method: 'POST' });
  await loadDashboard();  // 즉시 새로고침
}
```

### 6.3 API 호출 최적화

| 상황 | 전략 |
|------|------|
| 초기 로딩 | `Promise.all`로 병렬 호출 |
| Drilldown | 필요 시에만 그룹 정보 조회 |
| 액션 후 | 해당 위젯만 부분 새로고침 |
| 백그라운드 | 30초 간격 폴링 |

---

## 7. 구현 우선순위

### Phase 1: Critical (1주)

| Task | 난이도 | 설명 |
|------|--------|------|
| Health Summary 위젯 | 중 | 상태 계산 로직 + UI |
| Active Rollouts 버튼 추가 | 하 | Resume/Pause API 연동 |
| 불필요 위젯 제거 | 하 | 6개 위젯 숨김/제거 |
| 레이아웃 재구성 | 중 | 3-Layer 구조 적용 |

### Phase 2: High (1주)

| Task | 난이도 | 설명 |
|------|--------|------|
| Action Required 위젯 | 중 | 조치 필요 항목 집계 |
| Rollout Drilldown | 중 | 그룹별 상세 표시 |
| In-Progress 개선 | 중 | 지연 경고, 롤아웃명 표시 |

### Phase 3: Medium (1주)

| Task | 난이도 | 설명 |
|------|--------|------|
| Status Trend 개선 | 상 | 시간대별 집계 차트 |
| Action Activity 개선 | 중 | 병목 감지 로직 |
| 확인 다이얼로그 | 하 | 액션 전 확인 |

---

## 8. 수락 기준

### 8.1 전체 대시보드
- [ ] 30초 내 시스템 상태 파악 가능
- [ ] 별도 페이지 이동 없이 롤아웃 제어 가능
- [ ] 값이 0인 위젯은 자동 숨김
- [ ] 3-Layer 구조 적용됨

### 8.2 Health Summary
- [ ] SAFE/WARNING/CRITICAL 상태 표시
- [ ] 상태별 색상 및 아이콘 적용
- [ ] 상태 판정 사유 툴팁 표시

### 8.3 Active Rollouts
- [ ] Resume/Pause 버튼 동작
- [ ] 확인 다이얼로그 표시
- [ ] 롤아웃 전체 이름 표시 (툴팁 또는 말줄임 해제)
- [ ] 그룹 Drilldown 동작

### 8.4 Action Required
- [ ] 최대 5개 항목 표시
- [ ] 심각도순 정렬
- [ ] 항목 없을 때 위젯 숨김
- [ ] 클릭 시 상세 페이지 이동

---

## 9. 참고: API 필터 쿼리 예시

hawkBit은 FIQL (Feed Item Query Language) 사용:

```
# 상태가 running 또는 paused인 롤아웃
q=status==running,status==paused

# 상태가 error인 액션
q=status==error

# 특정 롤아웃의 액션
q=rollout.id==123

# 여러 조건 조합 (AND)
q=status==running;rollout.id==123

# 여러 조건 조합 (OR)  
q=status==running,status==paused
```

---

**문서 작성:** 운영팀  
**검토 요청:** Antigravity 개발팀

