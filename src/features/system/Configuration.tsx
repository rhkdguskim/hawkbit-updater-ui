import React from 'react';
import { Typography } from 'antd';

const Configuration: React.FC = () => {
    return <Typography.Title level={2}>System Configuration</Typography.Title>;
};

export default Configuration;
